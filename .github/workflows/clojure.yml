name: clojure

on:
  push:
    paths:
      - clojure/.devcontainer/**
      - .github/workflows/clojure.yml

jobs:
  CancelPrevious:
    runs-on: ubuntu-latest
    steps:
      - name: Cancel Old Pipeline
        uses: rokroskar/workflow-run-cleanup-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  BuildDevcontainer:
    needs: [CancelPrevious]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build Devcontainer
        run: |
          npm install -g @devcontainers/cli
          devcontainer build --workspace-folder clojure --image-name clojure-devcontainer

      - name: Show Disclaimer
        run: docker run --rm clojure-devcontainer sh /disclaimer.sh

      - name: Run Selftest
        run: docker run --rm clojure-devcontainer sh /selftest.sh
